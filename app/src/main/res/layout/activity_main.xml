<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btn_words"
        android:text="@string/words"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textSize="30sp"
        android:textColor="@color/blue"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btn_articles"
        android:text="@string/articles"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/btn_words"
        android:textSize="30sp"
        android:textColor="@color/blue"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btn_artcles_word"
        android:text="@string/articles_word"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/btn_articles"
        android:textSize="30sp"
        android:textColor="@color/blue"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btn_words_evaluate"
        android:text="@string/words_evaluate"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/btn_artcles_word"
        android:textSize="30sp"
        android:textColor="@color/blue"/>

</RelativeLayout>
